{
  "name": "http-writefile",
  "version": "1.0.0",
  "description": "Project of reading files to show html",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --watch index.js",
    "gitConfig":" git init && git branch -M main && git remote add origin %npm_config_repository% && git pull",
    "git" : "git add . && git commit -m %node_config_message% && git push -f origin main"
  },
  "author": "Joao Melo <joao.victor.ribeiro.melo@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.15"
  }
}
